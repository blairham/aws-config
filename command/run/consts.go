package run

const AwsProfile = "AWS_PROFILE"
const synopsis = "Runs a command with the appropriate aws credentials set"
const help = `
Usage: aws-sso-config run [options]

    Runs a command with the appropriate aws credentials set
`
